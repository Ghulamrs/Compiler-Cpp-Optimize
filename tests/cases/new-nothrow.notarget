tms6747  the emulator's runtime has no `std::set_new_handler` and no nothrow `operator new` - `<new>` declares them as TI's rts6740 exports them, and the emulator answers "undefined symbol" at the first. placement-array-new.cpp is the part of <new> that runs here.
