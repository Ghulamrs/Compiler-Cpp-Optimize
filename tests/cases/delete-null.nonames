x86_64-linux  the destructors and Poly's constructor are written inside their classes, so clang emits one comdat copy as C2/D2 and no C1 or D1 at all where cxx1 emits both of Itanium's forms - see docs/CONFORMANCE.md, "An inline member function is a strong symbol". x86_64-windows agrees on all 8, the deleting destructor ??_GPoly@@UEAAPEAXI@Z included, which is the one this case exercises through a null pointer
arm64-darwin  cxx1 guards `new Poly` since 2026-09-26 - [expr.new]/20, the storage freed if the constructor throws - so it emits the cleanup pad's __Unwind_Resume; Darwin's clang emits no cleanup for that allocation, though Linux's does for the same shape. The 11 other names agree.
