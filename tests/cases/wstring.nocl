This case includes this tree's own <string>, a basic_string<C> template whose members and the operators over it are inline and instantiated in the object - ??0?$basic_string@_W@std@@..., ??$?8_W@@..., __cxx1_length and the rest. cl's std::wstring is its own library's class with a different layout and different names, so the two objects share nothing to compare; every name both emit agrees.
